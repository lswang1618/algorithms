// Just rerender the partial, and it'll take care of the rest
$('#admin-table-campaigns').replaceWith('<%= escape_javascript render("table", campaigns:@campaigns) %>');
// If we wanted, we could do other stuff, too